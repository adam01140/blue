services:
  - type: web
    name: Form-Star
    env: docker
    # Use Docker to install LaTeX dependencies
    dockerfilePath: ./Dockerfile
    dockerContext: .
    # Remove buildCommand and startCommand - Dockerfile handles these
    # buildCommand: yarn && bash install-latex.sh  # REMOVED - using Docker instead
    # startCommand: node server.js  # REMOVED - Dockerfile CMD handles this

