# California Zip Code to County Lookup

A simple, beautiful web application that allows you to enter any California zip code and get the corresponding county name.

## Features

- Clean, modern user interface
- Real-time zip code validation
- Responsive design for mobile and desktop
- Fast lookup using local data

## How to Use

1. Open `index.html` in your web browser
2. Enter a 5-digit California zip code
3. Click "Lookup" or press Enter
4. The county name will be displayed

## Files

- `index.html` - Main HTML structure
- `styles.css` - Styling and layout
- `script.js` - JavaScript functionality
- `zipData.js` - Zip code to county mapping data (generated)
- `build-zip-data.js` - Regenerates `zipData.js` from `sources/`
- `courtData.js` - County to small claims court mapping
- `courtLookup.js` - ZIP -> county -> court helper functions

## Testing

Try these example zip codes:
- 90001 - Los Angeles County
- 94102 - San Francisco County
- 92101 - San Diego County
- 92602 - Orange County
- 95814 - Sacramento County

## Data

`zipData.js` covers every USPS ZIP code in California (about 2,600) and is
generated by `build-zip-data.js` from Census and USPS data. See
`DATA_SOURCES.md` for the sources, the decision rule for ZIPs that straddle
a county line, and how to rebuild it.

`courtData.js` maps all 58 counties to their superior court small claims
address. See `COURT_DATA_SETUP.md` for the lookup API.
